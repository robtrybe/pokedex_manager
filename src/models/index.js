const cardModel = require('./card.model');
const deckModel = require('./desk.model');
const deckCardModel = require('./deck.card.model');

module.exports = {
    cardModel,
    deckModel,
    deckCardModel,
}